{
  "name": "json_server",
  "version": "1.0.0",
  "main": "main.js",
  "scripts": {
    "start": "json-server --watch db.json --routes routes.json --port 3000",
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon main.js"
    
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "json-server": "^0.17.4",
    "json-server-extension": "^1.1.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  }
}
